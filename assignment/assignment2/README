BloomFilter contains implementation of Basic Bloom Filter
BlockedBloomFilter contains implementation of Blocked Bloom Filter

We have used python3 for implementation of Bloom Filter. Following are the libraries that are required for code execution.
	--bitarray
	--mmh3
	--numpy
	--_pickle
	--argparse
	--pandas

	Plots
	--seaborn
	--matplotlib

First execute run_bf.sh, so that appropriate alias is set

To execute BloomFilter use bf while for executing BlockedBloomFilter use bbf, and then pass accordingly the parameters
